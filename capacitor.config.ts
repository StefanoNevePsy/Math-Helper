import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.stefanonevepsy.mathhelper',
  appName: 'Math Helper',
  webDir: 'dist'
};

export default config;
